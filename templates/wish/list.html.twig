{% extends "base.html.twig" %}


{% block title %}{{ parent()}}
    - Liste
    
{% endblock %}

{% block body %}
    <section class="grid">
        {% for wish  in wishes %}
        <article class="card">
            <a href={{ path('wish_detail', {'id': wish.id}) }}>{{ wish.title }}
            </a>
        </article>
                {% endfor %}
    </section>

{% endblock %}
